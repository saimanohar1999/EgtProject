#include <egt/ui>
#include <iostream>
using namespace std;
using namespace egt;

int main(int argc, const char** argv)
{
  Application app(argc, argv);
  TopWindow window;

  boxSizer sizer(orientation::horizontal);

  window.show();
  return app.run();
}
