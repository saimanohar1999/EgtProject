






#include <egt/ui>
#include <iostream>
using namespace std;
using namespace egt;

int main(int argc, const char** argv)
{
  Application app;
  TopWindow window;
  //VideoWindow player(window.content_area(),
		  //"usr/share/egt/examples/video/microchip_corporate_mpeg2.avi");
  VideoWindow player(window.content_area(),
		  "file:/root/videop.mp4");
  window.add(player);
 // player.set_volume(8);
  player.show();
  bool 	play ();
  //void 	resize (const Size &size) override;
  player.play();
  window.show();

  return app.run();
}

